export type TProducts = Array<IProduct>;

export interface IProduct {
	id: string;
	article: string;
	title: string;
	yearOfPublication: number;
	shelfLife: string;
	category: string;
	number: number;
	printDate: string;
}

export interface IProduct {
	id: string;
	article: string;
	title: string;
	yearOfPublication: number;
	shelfLife: string;
	category: string;
	number: number;
	printDate: string;
}
